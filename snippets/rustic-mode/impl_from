# -*- mode: snippet -*-
#name : impl_from
#key : impl_from
#contributor : Robert Krahn
# --
impl From<${1:from}> for ${2:For} {
    fn from(${3:value}: $1) -> Self {
        Self {
            ${0:code}
        }
    }
}